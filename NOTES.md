Sending data (POST) with the fetch API
- youre going to grab the form using document.getElementByID('form')
- make an event listener
  - form.addEventListener('sumbmit', function (e) {
    e.perventdDefault()

    const = formData = new FormData(this)

    
  })